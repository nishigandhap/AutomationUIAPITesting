{
  "name": "automationtestingproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "allure:report": "allure generate allure-results --clean -o allure-report",
    "reportTest": "cypress run cypress/UI-APITest/**/*.js --headed --env allure=true && allure generate allure-results --clean -o allure-report"
    

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.35.2",
    "cypress": "^12.5.1"
  },
  "dependencies": {
    "@4tw/cypress-drag-drop": "^2.2.3",
    "@cypress/chrome-recorder": "^2.3.0",
    "allure-commandline": "^2.21.0"
  }
}
