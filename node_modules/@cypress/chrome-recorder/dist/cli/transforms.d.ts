declare type Flags = {
    force?: boolean;
    dry?: boolean;
    path?: string;
    print?: boolean;
};
export declare function runTransforms({ files, outputPath, flags, }: {
    files: string[];
    outputPath: string;
    flags: Flags;
}): Promise<Promise<string | void>[] | undefined>;
export {};
